const config = {
    styles: {
        stateActions: {
            default: {
                primary: 'background-color: blue; color: white; font-weight: bold; border-radius: 3px; padding: 6px;',
                secondary:
                    'background-color: #ee88ff; color: black; font-weight: bold; border-radius: 3px; padding: 6px;',
            },
        },
    },
}

export default config
